Open a DOS prompt or Unix shell and run the following command:

- Run the "java -cp .;./asjava.zip uojconnect" command for Windows
- Run the "java -cp .:./asjava.zip uojconnect" command for Unix

Note: Install JRE 1.5 or higher version and set the java.exe program path in the PATH environment