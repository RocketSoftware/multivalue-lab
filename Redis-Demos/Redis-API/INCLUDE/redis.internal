COMMON /redis/ redis$socket, redis$server, redis$port, redis$timeout, redis$connected, redis$database, redis$serverTimeout, redis$lastTime, redis$lastDate

EQU redis$port$default TO 6379
EQU redis$server$default TO "localhost"
EQU redis$timeout$default TO 0
EQU redis$serverTimeout$default TO 300
EQU redis$database$default TO 0
EQU BLOCKING$MODE TO 1

EQU redis$msg$separator TO CHAR(13):CHAR(10)
